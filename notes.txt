I am new to this service, so I am not certain about any of this. Based on what I
understand so far:

The recon batch calls the chargeback domain service. Before calling it, it picks up
the records with flag N, changes them to P, and then submits an async task to the
chargeback domain service.

Please go through the whole flow again and confirm or correct that.

THE TRANSACTION MANAGER

I am confused by the transaction manager in the recon batch config class, and
whether it is really needed.

- If it is managing a chunk-level transaction, what kind of transaction is it
  actually managing?
- What error scenario is it there for? What concretely happens if it is not there?
- We are already committing to the DB with @Transactional(propagation = REQUIRES_NEW),
  so that transaction commits on its own regardless.

Do you think the earlier failure -- the one we just fixed, where the flag was being
updated to P again -- was caused by this transaction manager? If not, what is the
actual cause, and how do we resolve it?

THE CONSTRAINT

You said this transaction manager is absolutely needed in order to call the chunk
method. If that is true, then how do we solve this problem?

The requirement is that the DB has to be updated immediately. We submit an async task
to the chargeback domain service, and that service needs to pick the record up. By
the time it picks it up, the flag must already be updated. We cannot wait until the
recon batch completes and only then commits P.

HOW I WANT YOU TO ANSWER

Dig deep. Do not just come back with questions. Go through the code, analyze
everything, and present me with the analysis.
